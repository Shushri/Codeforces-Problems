#include <bits/stdc++.h>using namespace std; using ll = long long;using vi = vector<ll>;using vl = vector<ll>;using pii = pair<int, int>;using pll = pair<ll, ll>;using vpii = vector<pii>;using vpll = vector<pll>;bool pwrof2(int n){    return (n>0 && (n&(n-1))==0);} void solve() {    ll n;    cin>>n;    int i=n-1;    for(i;i>=0;i--){        cout<<i<<" ";        if(pwrof2(i)){                        break;        }    }    for(int j=0;j<i;j++){        cout<<j<<" ";    }    cout<<endl;} int main() {    ios_base::sync_with_stdio(false);    cin.tie(NULL);    cout.tie(NULL);     ll t;    cin >> t;    while (t--) {        solve();    }    return 0;}