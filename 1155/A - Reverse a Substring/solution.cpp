#include <bits/stdc++.h>using namespace std; using ll = long long;using vi = vector<ll>;using vl = vector<ll>;using pii = pair<int, int>;using pll = pair<ll, ll>;using vpii = vector<pii>;using vpll = vector<pll>; void solve() {    int n;    cin>>n;    string s;    cin>>s;    int f=0;    for (int i=0;i<n-1;i++){                if((s[i+1]-'a')<(s[i]-'a')){            f=1;            cout<<"YES"<<endl;            cout<<i+1<<" "<<i+2<<endl;            return;        }    }    if(f==0){        cout<<"NO"<<endl;        return;    }     } int main() {    ios_base::sync_with_stdio(false);    cin.tie(NULL);    cout.tie(NULL);                 solve();        return 0;}