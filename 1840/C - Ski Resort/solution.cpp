#include <bits/stdc++.h>using namespace std;#define ll long long int int main(){  ll t;  cin>>t;  while(t--){    ll n,k,q;    cin>>n>>k>>q;    vector<ll> arr(n);    for (ll i=0;i<n;i++){      cin>>arr[i];    }    vector<ll> ans;    ll a=0;    for (ll i=0;i<n;i++){      if(arr[i]<=q){        a++;      }      else{        if(a>=k){          ans.push_back(a);        }        a=0;      }    }    if(a>=k){      ans.push_back(a);    }    ll sum=0;    for (ll el:ans){      ll f=el-k+1;      sum+=(f*(f+1))/2;    }    cout<<sum<<endl;   }}